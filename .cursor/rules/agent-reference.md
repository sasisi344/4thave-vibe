# Agent Reference Rule (Cursor)

- このプロジェクトで作業するAgentは、必ずルートの `CLAUDE.md` を先に参照する。
- 詳細ルールは次を参照する。
  - `.cursor/rules/editorial-guide.md`（執筆ルール）
  - `.claude/post-workflow.md`（清書・公開Workflow）
- 旧 `.agent` 配下のルールは廃止済み。参照先として使わない。
